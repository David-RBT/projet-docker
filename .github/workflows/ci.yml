name: David-projet-docker
on:
push: 
  branches: 
    - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    -name: david-projet-docker
    -uses: davidbekale/checkout@v2
    
    
      
      
     

 
      